# Spikes

Exploration reports from `/make-prd --explore` (template: `docs/templates/spike.md`). A spike never produces issues or ADRs; it ends in a *Promote to PRD?* gate. Spike code lives on `spike/<slug>` branches that are deleted, never merged.
